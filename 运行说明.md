# ModularGameVFX 运行说明

双击 `ModularGameVFX.uproject` 打开工程，默认地图为原来带怪物的 `/ModularGameVFXDemo/Maps/演示关卡`。点击运行后保留原角色、怪物、相机与技能栏。

- **F1**：打开特效调试 HUD；再次按 F1 或点击“返回战斗”收起。
- **W/S** 前后移动，**A/D** 左右平移，**Q/E** 也可平移。
- **左键** 选敌或拖动视角，**右键** 转向或攻击，滚轮缩放。
- **1～8**：原有八个技能；也可点击底部技能栏。
- **Tab** 切换目标，**Escape** 取消施法或清除目标。

调试面板打开期间暂停玩家键鼠战斗操作，怪物继续运行。面板用于选择目录条目、调整支持的参数、切换质量、预览和记录验收。关闭后恢复原操作。界面统一中文和 12 点字号；资源路径与游戏标签保留实际标识。

独立的 `/ModularGameVFXShowcase/Maps/L_VFX_Showcase` 仍保留，用于测试场景、相机、附着点、表面和独立压力区。

工程目录为 `F:/game/ModularGameVFX`。宿主构建模块使用 `ModularGameVFXHost`，核心插件仍为 `ModularGameVFX`，两者名称不会冲突。

本轮编译和原关卡运行验证见 `Saved/ProjectRename/delivery_verification.json`；构建日志为 `Saved/ProjectRename/build_final.log` 和 `build_font_fix.log`。运行检查不是人工特效质量验收或性能测量。
